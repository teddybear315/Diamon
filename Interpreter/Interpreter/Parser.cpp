#include <vector>
#include <string>
#include <sstream>
#include <fstream>
#include <algorithm>
#include <iostream>
#include "Essentials.h"

#define boolean bool

void parser(std::vector<std::string> lexedTokens) {
	
	for (int i = 0; i < lexedTokens.size(); ++i) {
		
	}

}